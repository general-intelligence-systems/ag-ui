# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# The published brute 3.0.0 predates the framework-agnostic rewrite
# (Brute::Message / Brute::ToolCall, MessageTransport) that this gem
# builds on — use the sibling checkout until the next brute release.
gem "brute", path: "../brute"
